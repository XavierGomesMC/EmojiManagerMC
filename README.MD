# A simple emoji manager for Java

If you want to check if a text is an emoji use
	EmojiManager.isEmoji(text).

If you want to get the Unicode HEX representation of an emoji use
	EmojiManager.getUnicodeHex(text).
